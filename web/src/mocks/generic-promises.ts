// Browser-compatible mock for Node.js promises modules (timers/promises, dns/promises)
// Required for zkemail-nr which uses these Node.js APIs
// These modules don't exist in browser environment, so we provide empty exports

export {}
